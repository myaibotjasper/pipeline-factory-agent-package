{
  "name": "pipeline-factory",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm -w @pipeline-factory/event-hub run dev",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint"
  }
}
